import { Validationtemplate } from './validationtemplate';

describe('Validationtemplate', () => {
  it('should create an instance', () => {
    expect(new Validationtemplate()).toBeTruthy();
  });
});
